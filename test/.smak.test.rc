# Minimal SMAK configuration for regression testing
# This file provides a clean baseline configuration for tests
# Usage: SMAK_RCFILE=test/.smak.test.rc ./run-regression

# Keep defaults - no custom user tweaks

# Print message to verify this rc file is being loaded (Perl code)
print "Using SMAK_RCFILE: $ENV{SMAK_RCFILE}\n" if $ENV{SMAK_RCFILE};
